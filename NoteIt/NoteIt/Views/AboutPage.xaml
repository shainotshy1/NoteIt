<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NoteIt.Views.AboutPage"
             xmlns:vm="clr-namespace:NoteIt.ViewModels"
             Title="{Binding Title}">
    
    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#96d1ff</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="4*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="8*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackLayout Grid.ColumnSpan="3" Orientation="Vertical" Spacing="10" VerticalOptions="End">
            <Label Text="{Binding IsRecording}" FontSize="16" Padding="0,0,0,0" HorizontalTextAlignment="Center"/>
        </StackLayout>
        <ImageButton x:Name="recordingButton" Grid.Row="1" Grid.Column="1" HorizontalOptions="Center" VerticalOptions="Start"
                BackgroundColor="Transparent" Source="{Binding ImageSource}" Scale="0.3" Clicked="recordingButton_Clicked" Command="{Binding Recording}"/>
    </Grid>

</ContentPage>
