<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:NoteIt.ViewModels"
             x:Class="NoteIt.Views.Settings"
             Title="{Binding Title}">
    
    <ContentPage.BindingContext>
        <vm:SettingsViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <Button Text="Back" CornerRadius="30" BackgroundColor="CornflowerBlue" TextColor="White" Command="{Binding BackCommand}" VerticalOptions="Center" HorizontalOptions="Center" WidthRequest="100"/>
    </ContentPage.Content>
</ContentPage>